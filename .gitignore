__pycache__/
*.py[cod]
*$py.class
download_log.txt
debug_params_v2.txt

# PyInstaller
build/
dist/
*.spec

# Inno Setup output (installer.iss writes FinFetcher-Setup.exe here) and the
# scratch file "build exe.bat" uses to hand the numeric version to it
dist_installer/
build_vernum.tmp

# FFmpeg binaries (download separately for builds)
ffmpeg/
version_info.txt

# Generated per build by make_build_info.py — committing it would ship one
# build's commit stamp inside every later build
build_info.json
